extends base

block content
    div
        p=posting.description
        small=posting.createdAt
    a(href=`${posting.id}/edit`) Edit Posting &rarr;
    br
    a(href=`${posting.id}/delete`) Delete Posting &rarr;
