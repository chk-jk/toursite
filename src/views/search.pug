extends base.pug
include mixins/posting

block content
    form(method="GET")
        input(placeholder="Search by title",name="keyword", type="text")
        input(type="submit", value="Search now")

    div
        each posting in posts 
            +posting(posting)